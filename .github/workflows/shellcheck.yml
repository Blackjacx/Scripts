# https://github.com/marketplace/actions/shellcheck
on:
  workflow_dispatch:
  push:
    branches:
      - '*'

name: "Shellheck"

jobs:
  shellcheck:
    name: Shellcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run ShellCheck
        uses: ludeeus/action-shellcheck@master
        with: 
          ignore_paths: actions .github .git
          ignore_names: \*.rb \*.zsh .zshrc