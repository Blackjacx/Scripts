# SKHD Config file
#
# Key Reference: https://github.com/koekeishiya/skhd/issues/1
# GitHub: https://github.com/koekeishiya/SKHD
# Example Config: https://github.com/koekeishiya/skhd/blob/master/examples/skhdrc
# Example Config: https://github.com/josean-dev/dev-environment-files/blob/main/.config/skhd/skhdrc

# stop/start/restart yabai
# ctrl + alt - r : yabai --restart-service

# --- Modify Layout ---

# balance out tree
hyper - e : yabai -m space --balance

# maximize a window
hyper - m : yabai -m window --toggle zoom-fullscreen

# toggle window float
hyper - f : yabai -m window --toggle float --grid 4:4:1:1:2:2

# flip along y-axis
# hyper - y : yabai -m space --mirror y-axis
#
# flip along x-axis
# hyper - x : yabai -m space --mirror x-axis

# --- Moving Windows Arround ---

# move window to display left and right
hyper - pageup : yabai -m window --display west yabai -m display --focus west
hyper - pagedown : yabai -m window --display east yabai -m display --focus east

# swap windows within space
# hyper - down : yabai -m window --swap south
# hyper - up : yabai -m window --swap north
# hyper - right : yabai -m window --swap east
# hyper - left : yabai -m window --swap west

# --- Changing Focus ---

# change window focus across displays
hyper - left : yabai -m window --focus west || "$(yabai -m display --focus west)"
hyper - down : yabai -m window --focus south || "$(yabai -m display --focus south)"
hyper - up : yabai -m window --focus north || "$(yabai -m display --focus north)"
hyper - right : yabai -m window --focus east || "$(yabai -m display --focus east)"

# --- macOS ---

# lcmd + space : open Raycast
# hyper - x : ~/.config/ghostty/toggle_ghostty.sh
